import { EmptyState } from '@/components/ui/empty-state';

export default function SourcesPage() {
  return (
    <EmptyState title="Sources">
      Web pages, books, videos, datasets, and repos worth keeping track of.
    </EmptyState>
  );
}
