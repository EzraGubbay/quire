import { EmptyState } from '@/components/ui/empty-state';

export default function ExperimentsPage() {
  return (
    <EmptyState title="Experiments">
      Runs, metrics, artifacts, and observations reported by the Python client.
    </EmptyState>
  );
}
