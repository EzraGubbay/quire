import { EmptyState } from '@/components/ui/empty-state';

export default function NotesPage() {
  return (
    <EmptyState title="Notes">
      Short linked Markdown pages with [[wiki links]], math, backlinks, and the graph.
    </EmptyState>
  );
}
