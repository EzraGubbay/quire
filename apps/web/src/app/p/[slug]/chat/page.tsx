import { EmptyState } from '@/components/ui/empty-state';

export default function ChatPage() {
  return (
    <EmptyState title="Chat">Ask questions over this project's documents, notes, and sources.</EmptyState>
  );
}
